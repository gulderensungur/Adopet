<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adopet">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pet_profile_for_petownerActivity" />
        <activity android:name=".AddMyPetActivity" />
        <activity android:name=".MyPetActivity" />
        <activity android:name=".SendMessage2" />
        <activity android:name=".AllMessages" />
        <activity android:name=".SendMessage" />
        <activity android:name=".Pet_Profile_forAdopter" />
        <activity android:name=".CreateProfileActivity" />
        <activity android:name=".PetOwnerActivity" />
        <activity android:name=".AdopterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".registerActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>